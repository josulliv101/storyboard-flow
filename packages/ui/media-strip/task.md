- [x] Update types, constants, and utils
  - [x] Document `__itemResult` phantom field in `media-strip.types.ts`
  - [x] Add runtime ID checks to `validateTimelineItemTiming` and `validateCollectionTimelineItem`
  - [x] Export DOM attribute constants in `media-strip.utils.ts`
  - [x] Implement `isElementFullyVisibleInScrollArea` in `media-strip.utils.ts`
  - [x] Refactor `areEqual` to whitelist keys in `media-strip.utils.ts`
- [x] Refactor factory boundaries for nominal safety
  - [x] Update factory input types in `media-strip.validation.ts` to accept `string` and validate them
- [x] Refactor board and keyboard reordering
  - [x] Expose `moveItem` core in `media-strip-board.tsx`
  - [x] Refactor `getAdjacentStripId` to use `compareDocumentPosition`
  - [x] Measure exact drag width in `handleDragStart` and thread to overlay
  - [x] Update `use-reorder-keyboard.ts` to use `moveItem` and descriptive cross-strip announcements
- [x] Refactor focus, scroll, and layout details
  - [x] Implement `MutationObserver` in `use-scroll-to-and-focus.ts`
  - [x] Reference shared DOM attribute constants in hooks and item component
  - [x] Use `isElementFullyVisibleInScrollArea` in `media-strip-item.tsx`
  - [x] Add comment for `item.id` dependency in `media-strip-item.tsx`
  - [x] Wrap `posterUrlsKey` in `useMemo` and memoize components in `media-strip-thumbnail.tsx`
  - [x] Remove Tailwind height coupling and use `THUMBNAIL_HEIGHT_PX` style in `media-strip-thumbnail.tsx`
  - [x] Update test IDs and add dnd transition comment in `media-strip.tsx`
- [x] Verification & Test Suite Completeness
  - [x] Add unit test checking `scroll < board` activation thresholds in `media-strip.types.test.ts`
  - [x] Add Storybook regression test story `MultiStripDragRegression` in `MediaStrip.stories.tsx`
  - [x] Implement 10 interaction tests via play functions in `MediaStrip.stories.tsx`:
    - [x] Keyboard reorder left/right within a strip
    - [x] Keyboard reorder up/down between two strips
    - [x] Escape restores item to original strip/index
    - [x] Pointer drag within same strip
    - [x] Pointer drag from strip A to strip B
    - [x] Pointer drag into an empty strip
    - [x] Selected item remains selected after reorder
    - [x] Virtualized offscreen item can be focused after ArrowRight/ArrowLeft navigation
    - [x] Dragging scroll area does not select an item
    - [x] Reorder handle arrow keys do not trigger selection navigation
  - [x] Verify all 186 Storybook interaction tests and 49 unit tests pass cleanly
